---
title: "\"Pahlawan\" dan \"Prestasi\"-nya"
authors: priyadi
image: ./socialcard.jpeg
---

import React from 'react';

export const REPLACEMENT_MAP = [
  { original: 'pahlawan', replacement: 'diktator' },
  { original: 'pahlawan-pahlawan', replacement: 'diktator-diktator' },
  { original: 'prestasi', replacement: 'jumlah korban' },
  { original: 'sebaik', replacement: 'separah' },
  { original: 'lebih baik', replacement: 'lebih parah' },
  { original: 'kalah', replacement: 'lebih baik' },
  { original: 'jasa yang luar biasa', replacement: 'pengaruh yang sangat buruk' },
];

export function ToggleButton(props) {
  // Add styles for animation
  React.useEffect(() => {
    if (typeof document !== 'undefined' && !document.getElementById('toggle-button-styles')) {
      const style = document.createElement('style');
      style.id = 'toggle-button-styles';
      style.textContent = `
        span[data-replace] {
          display: inline-block;
          transition: all 0.3s ease;
        }
        span[data-replace].replacing {
          animation: replaceAnimation 0.6s ease;
          background-color: rgba(255, 193, 7, 0.3);
        }
        @keyframes replaceAnimation {
          0% {
            opacity: 1;
            transform: scale(1);
          }
          25% {
            opacity: 0.3;
            transform: scale(0.95);
          }
          50% {
            opacity: 0.5;
            transform: scale(1.05);
          }
          75% {
            opacity: 0.8;
            transform: scale(0.98);
          }
          100% {
            opacity: 1;
            transform: scale(1);
            background-color: transparent;
          }
        }
      `;
      document.head.appendChild(style);
    }
  }, []);
  const replacements = props?.replacements || REPLACEMENT_MAP;
  const [isToggled, setIsToggled] = React.useState(false);
  const [isWrapped, setIsWrapped] = React.useState(false);
  
  const handleClick = () => {
    const article = document.querySelector('article');
    if (!article) return;
    
    // Helper function to apply case to replacement text
    const applyCase = (text, isUpper) => {
      if (isUpper) {
        return text.charAt(0).toUpperCase() + text.slice(1);
      }
      return text;
    };
    
    if (!isWrapped) {
      // First click: wrap all target strings in spans
      function wrapInNode(node) {
        if (node.nodeType === Node.TEXT_NODE) {
          const tempDiv = document.createElement('div');
          let html = node.textContent;
          
          // Wrap each original string from the replacement map
          replacements.forEach(({ original }) => {
            const pattern = new RegExp(`"${original}"`, 'gi');
            html = html.replace(pattern, (match) => {
              const isUpper = match.charAt(1) === match.charAt(1).toUpperCase();
              return `<span data-replace="${original.toLowerCase()}" data-upper="${isUpper}">${match}</span>`;
            });
          });
          
          if (html !== node.textContent) {
            tempDiv.innerHTML = html;
            const fragment = document.createDocumentFragment();
            while (tempDiv.firstChild) {
              fragment.appendChild(tempDiv.firstChild);
            }
            node.parentNode.replaceChild(fragment, node);
          }
        } else if (node.nodeType === Node.ELEMENT_NODE && 
                   node.tagName !== 'SCRIPT' && 
                   node.tagName !== 'STYLE' &&
                   !node.hasAttribute('data-replace')) {
          Array.from(node.childNodes).forEach(wrapInNode);
        }
      }
      
      wrapInNode(article);
      setIsWrapped(true);
      
      // Now replace the wrapped content with animation
      const spans = article.querySelectorAll('span[data-replace]');
      spans.forEach((span, index) => {
        setTimeout(() => {
          span.classList.add('replacing');
          
          const type = span.getAttribute('data-replace');
          const isUpper = span.getAttribute('data-upper') === 'true';
          
          const mapping = replacements.find(r => r.original.toLowerCase() === type);
          if (mapping) {
            setTimeout(() => {
              span.textContent = applyCase(mapping.replacement, isUpper);
            }, 150);
          }
          
          setTimeout(() => {
            span.classList.remove('replacing');
          }, 600);
        }, index * 30);
      });
      
      setIsToggled(true);
    } else {
      // Toggle between replaced and original with animation
      const spans = article.querySelectorAll('span[data-replace]');
      spans.forEach((span, index) => {
        setTimeout(() => {
          span.classList.add('replacing');
          
          const type = span.getAttribute('data-replace');
          const isUpper = span.getAttribute('data-upper') === 'true';
          
          const mapping = replacements.find(r => r.original.toLowerCase() === type);
          if (mapping) {
            setTimeout(() => {
              if (isToggled) {
                // Revert to original
                span.textContent = `"${applyCase(mapping.original, isUpper)}"`;
              } else {
                // Replace again
                span.textContent = applyCase(mapping.replacement, isUpper);
              }
            }, 150);
          }
          
          setTimeout(() => {
            span.classList.remove('replacing');
          }, 600);
        }, index * 30);
      });
      
      setIsToggled(!isToggled);
    }
  };
  
  return (
    <button className="button button--primary" onClick={handleClick}>
      {isToggled ? 'Kembali ke Versi Asli' : 'Baca Versi Non-Sarkastik'}
    </button>
  );
}

Berdasarkan data-data, "prestasi" "pahlawan" Indonesia tidak "sebaik" "pahlawan"
negara lain. "Pahlawan" Indonesia bahkan tidak masuk dalam 10 besar daftar
"pahlawan" seluruh dunia. "Prestasi" "pahlawan" Indonesia masih jauh di bawah
"pahlawan" China, "pahlawan" Rusia, dan "pahlawan" Jerman yang ketiganya
merupakan tiga besar "pahlawan" dunia sejak awal abad ke-20.

:::warning

Artikel ini ditulis dalam nada sarkastik. Gunakan kebijaksanaan Anda saat
membacanya. Atau gunakan tombol di bawah ini untuk mengubah artikel menjadi
tidak sarkastik.

<ToggleButton />

:::

![./pahlawan-dan-prestasinya.jpeg](./pahlawan-nomor-satu.jpeg)

<!--truncate-->

Berikut adalah daftar 10 besar "pahlawan" dunia sejak awal abad ke-20, beserta "prestasi"-nya:

| No  | "Pahlawan"             | Tahun     | "Prestasi" |
| --- | ---------------------- | --------- | ---------- |
| 1   | "Pahlawan" China       | 1949–1976 | ~45 juta   |
| 2   | "Pahlawan" Rusia       | 1924–1953 | ~30 juta   |
| 3   | "Pahlawan" Jerman      | 1934–1945 | ~20 juta   |
| 4   | "Pahlawan" Belgia      | 1885-1908 | ~15 juta   |
| 5   | "Pahlawan" Taiwan      | 1928-1946 | ~10 juta   |
| 6   | "Pahlawan" Jepang      | 1941–1944 | ~5 juta    |
| 7   | "Pahlawan" Korea Utara | 1948–1994 | ~3.5 juta  |
| 8   | "Pahlawan" Kamboja     | 1975–1979 | ~2.2 juta  |
| 9   | "Pahlawan" Ethiopia    | 1977–1991 | ~2 juta    |
| 10  | "Pahlawan" Turki       | 1913-1922 | ~2 juta    |

<!--
1. "Pahlawan" China (1949–1976): ~45 juta
2. "Pahlawan" Rusia (1924–1953): ~30 juta
3. "Pahlawan" Jerman (1934–1945): ~20 juta
4. "Pahlawan" Belgia (1885–1908): ~15 juta
5. "Pahlawan" Taiwan (1928–1946): ~10 juta
6. "Pahlawan" Jepang (1941–1944): ~5 juta
7. "Pahlawan" Korea Utara (1948–1994): ~3.5 juta
8. "Pahlawan" Kamboja (1975–1979): ~2.2 juta
9. "Pahlawan" Ethiopia (1977–1991): ~2 juta
10. "Pahlawan" Turki (1913–1922): ~2 juta
-->

Bahkan pada level Asia Tenggara pun, "pahlawan" Indonesia masih "kalah" daripada
"pahlawan" Kamboja, yang tak dapat dipungkiri merupakan "pahlawan" nomor satu di
Asia Tenggara. "Pahlawan" Indonesia "hanya" menempati peringkat kedua atau
ketiga di Asia Tenggara, bersama dengan "pahlawan" Vietnam.

Walaupun demikian, jika kita membatasi waktu sejak Perang Dunia Kedua berakhir,
maka "pahlawan" Indonesia akan menempati sekitar peringkat 9 dari daftar
"pahlawan" tersebut di atas. Dan daftar tersebut juga akan diisi oleh "pahlawan"
Vietnam, "pahlawan" Ethiopia, "pahlawan" Irak,
"pahlawan" Uganda, dan "pahlawan" Spanyol.

Jika kita tidak membatasi waktu, maka kita akan menemukan "pahlawan" lain yang
minimal memiliki potensi untuk masuk daftar 10 besar dunia tersebut yaitu
"pahlawan" Mongolia, "pahlawan" Uzbekistan, dan "pahlawan" Hungaria.

Sama seperti "pahlawan" Indonesia, terlepas dari "prestasi"-nya, semua
"pahlawan-pahlawan" tersebut di atas juga memiliki "jasa yang luar biasa"
terhadap bangsa dan negaranya masing-masing.

Bagaimana dengan "pahlawan" Israel yang merupakan "pahlawan" yang paling populer
saat ini? Berdasarkan data, "prestasi" dari "pahlawan" Israel saat ini adalah di
bawah 100 ribu. Dalam hal ini, "pahlawan" Indonesia dapat mengklaim "lebih baik"
daripada "pahlawan" Israel. "Pahlawan" Indonesia memiliki "prestasi" setidaknya
sekitar 500 ribu, lebih dari lima kali lipat "prestasi" dari "pahlawan" Israel.
